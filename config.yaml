preprocess:
  sample_rate: 16000
  chunk_sec: 10

# Default: spectral-only (fast, no model download).
# Enable wav2vec2 via --model flag on the CLI, or use_model=true in the API.
detectors:
  spectral:
    weight: 1.0

threshold: 0.5
